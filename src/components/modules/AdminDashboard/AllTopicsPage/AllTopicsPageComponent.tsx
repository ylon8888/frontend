import React from 'react';

const AllTopicsPageComponent = ({ chapterId }: { chapterId: string }) => {
    return (
        <div>
            hfggf
        </div>
    );
};

export default AllTopicsPageComponent;